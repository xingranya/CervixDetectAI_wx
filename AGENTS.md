---
trigger: always_on
alwaysApply: true
---

# AGENTS.md

## 交流与输出

- 对用户始终使用简体中文。
- 回答保持直接、低废话，先给结论和动作，不写泛化 checklist。
- 用户已经给出报错、行号、文件路径或明确预期时，直接在相关代码处处理，不做无效来回确认。

## 项目定位

- 本仓库是“云端智诊”微信原生小程序及其演示后端。
- 前端目录是 `miniprogram/`，后端目录是 `server/`，文档与提审材料在 `docs/`。
- 产品定位是“女性健康管理助手”，能力边界是健康信息记录、复查提醒、问题整理、健康知识浏览。
- 任何用户可见文案都必须符合上述定位，禁止写成在线诊断、治疗建议、处方、互联网问诊或医生替代。

## 目录速览

- `miniprogram/`：微信原生小程序前端，包含页面、分包、组件、配置与工具函数。
- `server/`：Node.js + Express + MySQL API 服务。
- `server/database/`：建表、升级、迁移脚本。
- `docs/`：提审和课程/比赛展示材料。

## 工作方式

- 先读代码再改代码，优先沿用仓库既有实现，不引入新框架、新目录结构或新风格。
- 结构分析优先使用 CodeGraph；文本搜索优先使用 `rg`。
- 禁止执行非必要的启动类终端操作。只有用户明确要求、或确实属于预览/PPT 类任务时，才允许后台启动服务。
- 不要主动执行长时间常驻命令、监听命令或开发服务器。
- 连续 3 次同类命令没有有效结果时，立即换思路，不继续硬试。
- 已经拿到足够证据后直接修改，不做重复验证。

## 代码风格

- 严格保持现有代码风格、命名方式、缩进和模块组织。
- 当前仓库以前端原生小程序 JS 与后端 CommonJS 为主；新增代码默认继续使用 CommonJS。
- 注释、文档说明、提交到仓库内的说明文字一律使用简体中文。
- 注释只解释必要的业务约束、兼容逻辑和非直观原因，不写废话注释。
- 前端展示文案必须真实、可直接上线，不许出现“待接入”“测试用”“示例数据”“请联系管理员”这类开发态口吻，除非当前页面本来就是后台或调试入口。

## 小程序前端约束

- 首屏应保持可浏览，不要把登录做成强制前置。
- 登录必须是用户主动触发，且先经过隐私/协议同意链路。
- 昵称和头像属于登录后的资料引导能力，不要重新挪回登录前强收集。
- `chooseAvatar` 返回的是临时文件路径，只能用于本地预览或恢复，不能直接作为持久 `avatarUrl` 落库。
- 持久头像地址必须以后端回传的 `http(s)` 地址为准；涉及头像问题时同时检查 `avatarUrl`、`avatarLocalPath`、`activeAvatarUrl` 的协作关系。
- 真机不能使用 `localhost`；接口基址统一检查 `miniprogram/config/app.js` 中的 `devtoolsApiBaseUrl`、`deviceApiBaseUrl`、`productionApiBaseUrl`。
- 页面、分包和预加载配置改动后，同时检查 `miniprogram/app.json` 的 `pages`、`subpackages`、`preloadRule` 是否一致。
- 隐私说明、服务协议、资料引导、反馈入口属于审核敏感区域；修改登录、头像、昵称、授权、订阅消息时，要联动检查这些页面和文案。

## 后端约束

- 后端入口是 `server/src/app.js`，小程序主路由在 `server/src/routes/miniapp.js`。
- 业务逻辑优先放在 `services` 与 `repositories`，不要把复杂逻辑直接堆在路由层。
- 新增或修改需要鉴权的接口时，保持现有鉴权边界，不要误把私有数据接口改成公开接口。
- 头像相关改动必须同时考虑 `server/src/services/avatar-storage.service.js`、`server/src/services/miniapp.service.js`、`server/src/repositories/miniapp.repository.js` 和静态资源暴露路径。
- 公开文件 URL 依赖 `MINIAPP_PUBLIC_BASE_URL`；涉及资源回显、真机访问或公网访问时必须考虑这个变量。
- 新增数据库字段或表结构时，至少同步更新初始化脚本和对应升级/迁移脚本，避免只改运行时代码。

## 审核与对外文案约束

- 对外品牌名优先使用“云端智诊”；出现 `CervixDetectAI` 时，先判断是否会暴露给审核或最终用户。
- 审核高风险点优先级高于普通 UI 文案：登录时机、是否强制授权、是否强制收集头像昵称、类目一致性、隐私说明、服务边界。
- 任何页面都不要暗示在线诊疗、医生实时回复、医学结论生成或报告判读结论。
- 订阅消息只能在用户主动触发时请求授权，不能静默或首次进入自动申请。

## 配置与密钥

- 微信 AppID、AppSecret、AI Key、Webhook Secret、数据库密码等敏感信息应只放在环境变量或本地配置，不要写回源码、文档截图或示例文案。
- 修改小程序配置时，优先检查：
  - `miniprogram/project.config.json`
  - `project.config.json`
  - `miniprogram/config/app.js`
  - `server/src/config/env.js`
- 改完配置类文件后，重新读取文件确认变更没有被工具回弹。

## 修改前后的检查原则

- 改动前先确认影响面：页面路由、分包、公共工具函数、接口返回结构、数据库字段、审核文案。
- 改动后优先做低成本静态校验，不在未被要求时主动拉起整套服务。
- 小程序 JS 改动后，优先检查相关页面和工具函数是否存在路径、字段名或生命周期不一致。
- 后端 JS 改动后，优先执行 `server/package.json` 已定义的静态检查命令。

## 推荐校验命令

- 后端语法校验：
  ```bash
  cd server && npm run check
  ```
- JSON 配置校验：
  ```bash
  jq empty miniprogram/app.json miniprogram/project.config.json project.config.json
  ```
- 仓库文本扫描：
  ```bash
  rg -n "CervixDetectAI|wx[0-9a-zA-Z]{10,}|appid|chooseAvatar|privacy|MINIAPP_PUBLIC_BASE_URL" miniprogram server docs
  ```
- 涉及单个 JS 文件时，可直接：
  ```bash
  node --check 路径/到/文件.js
  ```

## 常见任务的默认处理方式

- 修头像真机显示问题：先查临时路径是否被持久化，再查前端缓存字段、后端回包归一化和 `/uploads/avatars` 公开地址。
- 修登录/授权问题：先查是否违反“先浏览、后主动登录、再资料引导”的流程，不要先从 UI 表象下手。
- 修审核问题：先查首页可达性、登录时机、隐私协议、品牌名、服务边界，再查代码细节。
- 改接口字段：同时核对前端 `request` 调用方、服务层组装逻辑、仓储 SQL、数据库脚本和缓存读写。
- 改页面或分包：同时核对 `app.json`、导航工具、页面路径常量和预加载规则。

## 禁止事项

- 禁止无依据地大范围重构。
- 禁止为了局部修复引入新的全局状态或重复工具函数。
- 禁止把临时调试值、测试域名、测试账号提示直接提交到用户可见界面。
- 禁止在未获用户要求的情况下启动开发者工具、Node 服务、数据库服务或任何常驻进程。

## 交付标准

- 改动应当最小且完整，优先修根因，不只修表层现象。
- 涉及用户可见流程时，同时保证文案、数据结构和异常提示一致。
- 最终说明中只汇报实际改动、验证结果和仍存在的风险，不写空泛总结。

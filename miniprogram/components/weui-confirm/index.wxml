<t-dialog
  visible="{{show}}"
  title="{{title}}"
  content="{{content}}"
  cancel-btn="{{cancelText || '取消'}}"
  confirm-btn="{{computedConfirmBtn}}"
  bind:cancel="handleCancel"
  bind:confirm="handleConfirm"
/>

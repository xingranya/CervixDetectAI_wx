<view class="page detail-page">
  <view wx:if="{{pageStatus === 'loading'}}" class="card state-card">
    <text class="state-title">正在加载记录详情</text>
    <text class="state-desc">请稍候，正在整理这条检查摘要。</text>
  </view>

  <view wx:elif="{{pageStatus === 'error'}}" class="card state-card">
    <text class="state-title">暂时无法加载记录详情</text>
    <text class="state-desc">{{errorMessage}}</text>
    <button class="primary-button" bind:tap="reloadDetail">重新加载</button>
  </view>

  <view wx:elif="{{record}}" class="card detail-card">
    <text class="pill">{{record.date}}</text>
    <view class="detail-title-row">
      <view class="icon-chip">
        <image src="/assets/icons/detail-active.png" mode="aspectFit" />
      </view>
      <text class="detail-title">{{record.title}}</text>
    </view>
    <view class="detail-info">
      <view class="detail-row">
        <text class="detail-label">检查项目</text>
        <text class="detail-value">{{record.project}}</text>
      </view>
      <view class="detail-row">
        <text class="detail-label">摘要</text>
        <text class="detail-value">{{record.summary}}</text>
      </view>
      <view class="detail-row">
        <text class="detail-label">提醒</text>
        <text class="detail-value">{{record.suggestion}}</text>
      </view>
    </view>
    <view class="detail-actions">
      <button class="ghost-button" bind:tap="editRecord">
        <image class="button-icon" src="/assets/icons/form-active.png" mode="aspectFit" />
        编辑记录
      </button>
      <button class="delete-button" bind:tap="deleteRecord">
        <image class="button-icon" src="/assets/icons/trash-danger.png" mode="aspectFit" />
        删除记录
      </button>
    </view>
  </view>

  <view class="safe-note">如对检查内容有疑问，请携带记录到线下医疗机构咨询专业人员。</view>
</view>
